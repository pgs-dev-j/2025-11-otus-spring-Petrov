spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-test.yaml
  shell:
    interactive:
      enabled: false